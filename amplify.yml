version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Añadimos sudo para ejecutar con privilegios de administrador
        - sudo yum install -y gcc-c++ make
        - rm package-lock.json
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
