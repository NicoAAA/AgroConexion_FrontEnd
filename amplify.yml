version: 1
frontend:
  phases:
    preBuild:
      commands:
        - sudo yum install -y gcc-c++ make
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
